{% extends 'base.html.twig' %}

{% block title %}Influence{% endblock %}

{% block body %}
    <h1>Influence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ influence.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ influence.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_influence_index') }}">back to list</a>

    <a href="{{ path('app_influence_edit', {'id': influence.id}) }}">edit</a>

    {{ include('influence/_delete_form.html.twig') }}
{% endblock %}
