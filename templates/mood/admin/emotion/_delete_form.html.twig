<form method="post"
      action="{{ path('app_admin_emotion_delete', {'id': emotion.id}) }}"
      class="js-confirm-delete-form"
      data-confirm-title="Confirmer la suppression"
      data-confirm-message="Êtes-vous sûr de vouloir supprimer cette émotion ?">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emotion.id) }}">
    <button class="sn-admin-action sn-admin-action-danger sn-admin-btn-block" type="submit">
        Delete
    </button>
</form>
